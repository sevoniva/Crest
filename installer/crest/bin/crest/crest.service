[Unit]
Description=Crest Service
After=docker.service
Requires=docker.service

[Service]
User=root
Group=root

Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/crestctl start
ExecStop=/usr/bin/crestctl stop
ExecReload=/usr/bin/crestctl reload

[Install]
WantedBy=multi-user.target
